{
  "name": "tool2agent",
  "version": "0.0.0",
  "private": true,
  "description": "tool2agent monorepo",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @tool2agent/types build && pnpm --filter @tool2agent/ai build && pnpm --filter @tool2agent/schemas build",
    "test": "pnpm --filter @tool2agent/types test && pnpm --filter @tool2agent/schemas test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "pnpm --filter @tool2agent/types clean && pnpm --filter @tool2agent/ai clean && pnpm --filter @tool2agent/schemas clean"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=18"
  }
}
